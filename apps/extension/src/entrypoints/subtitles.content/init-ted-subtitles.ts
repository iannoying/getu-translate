/**
 * Minimal bootstrap stub for TED subtitles.
 *
 * Task 3 layers on the actual `tryInit` + navigation listeners; this stub
 * exists so the `tedHandler` registered in Task 2 has a callable `init`.
 */
export function initTedSubtitles() {
  // Intentionally empty; replaced in Task 3.
}
