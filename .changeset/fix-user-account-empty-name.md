---
"@getu/extension": patch
---

fix(ui): show the email local-part (before `@`) in the popup header when the signed-in user has no display name (e.g. email-OTP accounts) instead of rendering nothing
